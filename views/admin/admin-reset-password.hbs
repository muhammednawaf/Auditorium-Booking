<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password - Farook College Auditorium</title>
    <link rel="stylesheet" href="/stylesheets/forgot-credential.css">
    
</head>
<body>
    <div class="reset-container">
        <div class="reset-header">
            <h1>Reset Password</h1>
            <p>Enter your email and new password</p>
        </div>

        <div id="errorMessage" class="error-message"></div>
        <div id="successMessage" class="success-message"></div>

        <form action="/admin/reset-password" id="resetForm" method="post">
           

            <div class="form-group">
                <label for="confirmPassword">Reset Token</label>
                <input
                    type="password"
                    id="token"
                    name="token"
                    required
                    autocomplete="new-password"
                    placeholder="Reset Token"
                >
                <span class="password-toggle" onclick="togglePassword('confirmPassword')">👁️</span>
            </div>
            <div class="form-group">
                <label for="confirmPassword">New Password</label>
                <i>🔒</i>
                <input
                    type="password"
                    id="newPassword"
                    name="newPassword"
                    required
                    autocomplete="new-password"
                    placeholder="New Password"
                >
                <span class="password-toggle" onclick="togglePassword('confirmPassword')">👁️</span>
            </div>
            

            <button type="submit" class="btn-reset" onclick="handleReset(event)">
                Reset Password
                <span class="loading"></span>
            </button>
        </form>

        <div class="reset-footer">
            <a href="/login">← Back to Login</a>
        </div>
    </div>
    

</body>
       <script src="/javascripts/admin-forgot-credential.js"></script>
</html>